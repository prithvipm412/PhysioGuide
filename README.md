# PhysioGuide
PhysioGuide  uses real-time computer vision to turn a laptop or phone webcam into a form-correction coach. It runs Google's MediaPipe Pose Landmarker directly in the browser, tracking 33 body landmarks per frame entirely on-device — no video leaves the user's machine.
